 ==========================================================================
 Rails 8 PARITY — completed
==========================================================================

Branch: upgrade-again
Done (committed): Rails 2.3 → 8 port, Hotwire/Turbo/Tailwind UI, RJS → Turbo Streams.
Done (this pass): event autocomplete refresh, bucket-merge line-item
                  consolidation, controller-test conversion and shim/gem
                  removal, reference documentation refresh, Rails 8
                  migration/server-start compatibility, and demo login repair.
Tests: bin/rails test = 304 runs, 1176 assertions, green. System tests were
       removed; browser behavior is verified by ad hoc manual testing.

------------------------------------------------------------------------
1. BLOCKING — front end must actually boot                         [DONE]
------------------------------------------------------------------------

* [x] Stimulus boot covered by test/unit/fe_boot_test.rb server-side
      wiring/importmap assertions. The former headless-Chrome
      test/system/events_form_test.rb was removed; browser behavior is
      now verified by ad hoc manual testing.
* [x] Event recall: already Stimulus fetch-JSON; dead format.js
      (Events.doneLoadingRecalledEvents) deleted from events#index.
* [x] Dormant format.js blocks deleted (events/accounts/buckets/tags/render_404).

------------------------------------------------------------------------
2. VIEWS — drop HAML, go full Rails 8 ERB (43 haml → 0)            [DONE]
------------------------------------------------------------------------

* [x] Convert all app/views/**/*.haml to idiomatic ERB. (43 converted, 0 left)
* [x] Replace form_for / form_tag with form_with (9 call sites).
* [x] Remove Rails-2-era helpers ($" + input, .html_safe traps).
* [x] Remove gems: haml, haml-rails; drop config/initializers/haml_fix.rb.
* [x] json_fix.rb KEPT — still required for json 3.x quirks_mode /
      ActiveSupport::JSON.decode of encrypted session cookies (not XML-related).

------------------------------------------------------------------------
3. CONTROLLERS / HELPERS — idiomatic Rails 8                       [DONE]
------------------------------------------------------------------------

* [x] respond_to audit; unused single-format blocks flattened; format.xml removed.
* [x] QueryFilter string/symbol key bug fixed (+ tests).
* [x] Events#index bare /events: HTML → redirect to root subscription;
      API → 400 JSON (+ tests).
* [x] LegacyControllerTestHelpers and LegacyAssertResponse removed after the
      integration-test conversion; `rails-controller-testing` gem removed.
* [x] shims.rb slimmed: dropped form_for + Haml patches; kept version loader +
      Errors#each_full (used by accounts/_form). h() shim dropped (Rails has it).
* [x] XML REST API gutted: format.xml removed from 7 controllers, Model#to_xml
      removed, 54 test requests + 39 Hash.from_xml converted to JSON,
      activemodel-serializers-xml + rexml gems dropped.

------------------------------------------------------------------------
4. PROJECT HYGIENE — look like a fresh rails new app               [DONE]
------------------------------------------------------------------------

* [x] Rails 2 leftovers already gone (prior commit); README.md recreated.
* [x] pg2sqlite.py — never existed on disk/history; nothing to do.
* [x] Swap file already absent.
* [x] .gitignore: added .DS_Store, *.swp (storage/log/tmp/builds already covered).
* [x] Push upgrade-again when green.

------------------------------------------------------------------------
5. KNOWN PRODUCT BUGS (pre-existing, not Rails-8-specific)               [DONE]
------------------------------------------------------------------------

* [x] New tag/actor autocomplete refreshes after event creation without reload.
* [x] Bucket merges net same-event line items that collapse onto one bucket
      (any roles); pairs netting to zero are removed, and events left with
      no legs (e.g. a reallocation onto its own bucket) are dropped.

------------------------------------------------------------------------
6. TEST SUITE MODERNIZATION — Fizzy-style integration tests          [DONE]
------------------------------------------------------------------------
Completed: converted all 8 former test/functional/* files (137 tests, 1162 LOC) from
ActionController::TestCase + Rails-2 positional/xhr style into
ActionDispatch::IntegrationTest hitting real routes; deleted both legacy shims
and the rails-controller-testing gem, and closed the coverage gaps.
The former `test/functional/` files now live in `test/controllers/`.

Current check (2026-09-23): bin/rails test = 304 runs, 1176 assertions, green.
System tests were removed in favor of ad hoc browser testing (no Chrome
dependency).

ENVIRONMENT (all verified, required to run anything):
  * Ruby must be put on PATH explicitly, otherwise Homebrew Ruby 4.0.7 is
    picked up and Bundler raises Bundler::GemNotFound:
      export PATH=~/.local/share/mise/installs/ruby/3.4.8/bin:$PATH
  * Parallel work isolates the SQLite test DB per agent via:
      TEST_ENV_NUMBER=<name> bin/rails db:test:prepare
      TEST_ENV_NUMBER=<name> PARALLEL_WORKERS=1 bin/rails test <files>
    -> uses storage/test<name>.sqlite3. Without this, concurrent runs
       clobber storage/test.sqlite3.
  * Behavioral reference is the read-only `../bucketwise-master` worktree
    (detached at `a12e77c`); `git show master:<path>` also works.
  * Style reference: ~/dev/fizzy.
  * `bin/rails test` DOES discover test/controllers/ (verified).

DONE SO FAR:
  * [x] SessionHelpers in test/test_helper.rb — sign_in(:john) does a real
        POST /session, sign_out, and basic_auth_headers(:john, password:).
        Verified by test/integration/session_helpers_test.rb.

CONVERSION MAPPING (apply uniformly):
  get :show, :id => x             -> get event_path(x)
  post :create, :subscription_id… -> post path, params: { ... }
  xhr :put, :update, …            -> put path, params: { ... }, as: :turbo_stream
  :format => "json"               -> format: :json (or ".json" path suffix)
  assert_response :missing        -> assert_response :not_found
  assert_template "accounts/show" -> assert_select / body match on real HTML
  assigns(:subscription)          -> assert observable effect (DB / rendered HTML)
  login! :john, login_default_user-> sign_in :john
  api_login! :john, "testing"     -> headers: basic_auth_headers(:john)
  session[:user_id] assertions    -> assert the redirect, then follow_redirect!
  Run `bin/rails routes` for exact path helpers; never guess them.

COMPLETED TASKS (A–D were independent and used disjoint files):
  * [x] A — events:      test/functional/events_controller_test.rb
                         (390 LOC, 43 requests) -> test/controllers/
  * [x] B — accounts + buckets: 156 + 154 LOC   -> test/controllers/
  * [x] C — tags + statements + subscriptions + tagged_items + sessions
                         172 + 102 + 79 + 68 + 41 LOC -> test/controllers/
  * [x] D — coverage gaps closed: helper tests, actor/subscription/user_subscription
        tests, Populator and concern tests, POST password-change coverage, and
        pagination coverage; no SimpleCov or new gems added.
  * [x] E — AFTER A–D: deleted LegacyControllerTestHelpers +
        LegacyAssertResponse, removed test/functional/ entirely, removed
        "rails-controller-testing" from the Gemfile; `bin/rails test` is green.
        Headless-Chrome system tests were later removed in favor of ad hoc
        browser testing.
  * [x] F — refreshed AGENTS.md test-layout facts, Fizzy path, and legacy
        reference instructions (read-only `../bucketwise-master` worktree;

------------------------------------------------------------------------
7. MIGRATIONS AND SERVER STARTUP                                      [DONE]
------------------------------------------------------------------------
  * [x] Port all legacy migrations to versioned ActiveRecord::Migration[8.0]
        classes and instance up/down methods.
  * [x] Fix the initial migration rollback table name and make actor
        normalization independent of current application model callbacks.
  * [x] Preserve legacy nullable timestamps and refresh `db/schema.rb` to
        reflect the indexes produced by a fresh migration.
  * [x] Verify a fresh `db:migrate`, `db:rollback STEP=9`, and re-migration.
  * [x] Verify Puma starts and responds on `/` and `/session/new` without a
        pending-migrations error.

------------------------------------------------------------------------
8. DEMO LOGIN                                                           [DONE]
------------------------------------------------------------------------
  * [x] Document the actual `demo:build` task and `bw.demo` / `demo`
        credentials.
  * [x] Supply a required credit-card limit so demo building succeeds, with an
        integration test exercising the build and real login endpoint.

KNOWN HAZARDS (do not paper over these):
  * sessions#destroy: the old test called `get :destroy`, but routes only
    expose DELETE /session — the controller test bypassed routing entirely.
    Use delete session_path and check the layout's logout link to confirm
    what the app really does.
  * Every old test passes WITHOUT routing. A converted test may fail for a
    real reason. Treat that as a finding: consult `git show master:...` for
    intended behavior, and never weaken an assertion just to get green.
  * `assigns(...)` has no integration equivalent — assert what a user can
    actually observe (HTML, redirects, database state).

========================================================================
Known route gaps (documented by smoke/probe, not fixed — missing views/actions):
  406 HTML: subscriptions/:id/{accounts,events,tags} index, tags#new,
            accounts/:id/events, events/:id show (turbo_stream only)
  404: accounts#edit, buckets#edit, tags#edit, subscriptions#new/#edit
       (no actions/views — UI does not link them)
  404 nil-container: top-level /accounts /buckets /tags /statements /events/new
==========================================================================
